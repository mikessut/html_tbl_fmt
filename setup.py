from setuptools import setup

setup(name='html_tbl_fmt',
      version='0.1',
      description='Format and conditional format of HTML tables',
      packages=['html_tbl_fmt'],
      install_requires=['lxml']
      )